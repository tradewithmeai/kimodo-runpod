# Copy to .env and fill in. .env is gitignored — never commit the real key.
# RunPod API key (backup account). Create at https://console.runpod.io/user/settings
RUNPOD_API_KEY=rpa_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
